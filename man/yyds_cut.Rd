% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yyds_cut.R
\name{yyds_cut}
\alias{yyds_cut}
\title{便捷切割函数}
\usage{
yyds_cut(
  data,
  vars,
  value = NULL,
  probs = NULL,
  ncut = NULL,
  suffix = "q",
  right = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{数据框，包含需要切割的变量。}

\item{vars}{需要进行切割的变量名称（字符向量）。}

\item{value}{指定自定义的切割点。如果提供，则忽略 \code{probs} 和 \code{ncut} 参数。}

\item{probs}{分位数，指定根据分位数进行切割（例如 \code{probs = c(0.25, 0.5, 0.75)}）。}

\item{ncut}{通过指定切割的数量来进行分组（例如 \code{ncut = 4} 进行四分位切割）。}

\item{suffix}{新生成的变量名称的后缀，默认为 "q"（如变量 \code{age} 切割后生成变量 \code{ageq}）。}

\item{right}{是否将区间右端点包含在内，默认为 \code{TRUE}，即(a,b]。}

\item{verbose}{是否在控制台输出详细信息，默认为 \code{TRUE}。}
}
\value{
返回带有新切割变量的数据框。新变量名为原变量名加上 \code{suffix} 后缀。
}
\description{
本函数用于对数据框中的变量进行分组切割，根据指定的断点或分位数，将变量划分为多个类别。
支持通过 \code{value}、\code{probs} 或 \code{ncut} 参数指定切割标准。
}
\examples{
\dontrun{
# 示例数据
set.seed(42)
data <- data.frame(
  age = sample(18:80, 100, replace = TRUE),
  income = sample(10000:100000, 100, replace = TRUE)
)

# 使用分位数进行切割
data_cut <- yyds_cut(data, vars = c("age", "income"), probs = c(0.25, 0.5, 0.75))
}

}

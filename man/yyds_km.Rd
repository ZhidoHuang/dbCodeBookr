% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yyds_km.R
\name{yyds_km}
\alias{yyds_km}
\title{Kaplan-Meier 曲线与竞争风险累计发生率曲线（CIF）绘图}
\usage{
yyds_km(
  data,
  time_var,
  outcome,
  exposure,
  adjusted = NULL,
  xlim = NULL,
  ylim = NULL,
  legend_position = "top",
  x_breaks = NULL,
  n_times = 100,
  risk_table = FALSE,
  show_p = TRUE,
  p_x = NULL,
  p_y = NULL,
  p_digits = 3,
  y_percent_accuracy = 1,
  line_width = 1.1,
  palette = NULL,
  title = NULL,
  x_lab = "Follow-up time",
  y_lab = NULL
)
}
\arguments{
\item{data}{数据框，包含随访时间、结局变量、暴露/分组变量以及可能的调整变量。}

\item{time_var}{字符，随访时间变量名。}

\item{outcome}{字符，结局变量名。
KM 曲线要求编码为 0/1，其中 0 = 删失，1 = 事件。
CIF 曲线要求编码为 0/1/2，其中 0 = 删失，1 = 目标事件，2 = 竞争事件。
当前目标事件固定为 \code{cause = 1}。}

\item{exposure}{字符，分组变量名。可以是因子、字符或数值变量；绘图时会按该变量分组。}

\item{adjusted}{NULL 或字符向量。默认为 \code{NULL}，表示未调整。
若传入协变量名向量，则绘制调整后的模型预测曲线。
注意：\code{adjusted} 中不需要包含 \code{exposure}，函数会自动将 \code{exposure} 加入模型。}

\item{xlim}{数值向量，长度为 2，表示 x 轴显示范围，如 \code{c(0, 120)}。
默认从 0 到分析数据中的最大随访时间。}

\item{ylim}{数值向量，长度为 2，表示 y 轴范围，如 \code{c(0, 1)}。
若为 \code{NULL}，函数会根据曲线最大值自动生成较美观的 y 轴上限与刻度。}

\item{legend_position}{图例位置。默认 \code{"top"}，也可设为 \code{"bottom"}、\code{"left"}、
\code{"right"} 或 \code{"none"}。}

\item{x_breaks}{数值向量，x 轴刻度位置。
该参数同时控制主图 x 轴刻度和风险表格的统计时间点，因此建议显式指定，
例如 \code{seq(0, 120, by = 24)}。
若为 \code{NULL}，函数会基于 \code{xlim} 使用 \code{pretty()} 自动生成。}

\item{n_times}{整数，模型预测时间点数量。主要用于调整后的 Cox/Fine-Gray 曲线，
也用于未调整 CIF 曲线的预测网格。默认 100。数值越大曲线越细，但计算可能更慢。}

\item{risk_table}{逻辑值，是否在主图下方添加 number at risk 表格。
默认 \code{FALSE}。风险表格的列位置由 \code{x_breaks} 决定。}

\item{show_p}{逻辑值，是否显示未调整曲线的组间比较 P 值。默认 \code{TRUE}。
仅在 \code{adjusted = NULL} 时生效；调整后曲线不显示 P 值。
KM 曲线显示 Log-rank P 值，CIF 曲线显示 Gray's test P 值。}

\item{p_x, p_y}{数值，P 值标签的 x 和 y 坐标。若为 \code{NULL}，函数会自动放置。
默认 x 坐标为 \code{xlim[1]}，y 坐标位于 y 轴范围约 95\% 高度处。}

\item{p_digits}{整数，P 值显示的小数位数。默认 3。
例如小于 \code{0.001} 时显示为 \code{P < 0.001}。}

\item{y_percent_accuracy}{数值，y 轴百分比标签精度。默认 1，表示按 1\% 显示。
若希望显示到 0.1\%，可设为 \code{0.1}。}

\item{line_width}{数值，曲线线宽。默认 1.1。}

\item{palette}{字符向量，自定义颜色。可以是未命名向量，顺序需与分组水平一致；
也可以是命名向量，名称与分组名称对应。}

\item{title}{字符，图形主标题。默认 \code{NULL}。}

\item{x_lab}{字符，x 轴标题。默认 \code{"Follow-up time, months"}。}

\item{y_lab}{字符，y 轴标题。若为 \code{NULL}，函数自动设置：
KM 为 \code{"Cumulative event probability"}，CIF 为 \code{"Cumulative incidence"}。}
}
\value{
返回一个列表，包含：
\describe{
\item{plot}{组合后的 ggplot 对象；若 \code{risk_table = TRUE}，则包含主图和风险表格。}
\item{curve_plot}{仅包含曲线的 ggplot 对象。}
\item{risk_table_plot}{风险表格的 ggplot 对象；若未绘制风险表格则为 \code{NULL}。}
\item{risk_table_data}{风险表格数据框；若未绘制风险表格则为 \code{NULL}。}
\item{data}{用于绘图的曲线数据框，包含时间、估计值、分组和模型类型。}
\item{model}{拟合模型对象，可能为 \code{survfit}、\code{coxph}、\code{prodlim} 或 \code{FGR}。}
\item{plot_data}{实际用于分析的数据，即删除相关变量缺失值后的数据。}
\item{curve_type}{字符，\code{"km"} 或 \code{"cif"}。}
\item{adjusted}{逻辑值，表示是否进行了协变量调整。}
\item{xlim}{实际使用的 x 轴范围。}
\item{x_breaks}{实际使用的 x 轴刻度，同时也是风险表格统计时间点。}
\item{p_value}{未调整组间比较的 P 值；调整后曲线或未显示 P 值时为 \code{NA}。}
\item{p_label}{图中显示的 P 值标签；若未显示则为 \code{NULL}。}
}
}
\description{
根据结局变量编码自动选择绘图方法：结局为 0/1 时绘制 Kaplan-Meier
累计事件概率曲线，结局为 0/1/2 时绘制竞争风险累计发生率曲线（CIF）。
函数支持未调整曲线和协变量调整后的模型预测曲线，并可选显示风险表格。
}
\details{
对于 0/1 结局：
\itemize{
\item 未调整时使用 \code{survival::survfit()} 绘制 \code{1 - S(t)}。
\item 调整后使用 \code{survival::coxph()} 拟合 Cox 模型，并通过边际标准化预测累计事件概率。
}

对于 0/1/2 结局：
\itemize{
\item 未调整时使用 \code{prodlim::prodlim()} 绘制目标事件的 CIF。
\item 调整后使用 \code{riskRegression::FGR()} 拟合 Fine-Gray 模型，并通过边际标准化预测 CIF。
}

调整后曲线采用边际标准化思想：对每个分组水平，函数将所有个体的
\code{exposure} 设置为该水平，同时保留其他协变量的原始分布；随后预测每个个体
在各时间点的累计风险或 CIF，并对所有个体取平均。因此调整后曲线表示：
在当前分析人群协变量分布下，若所有人都处于某一暴露组时的平均预测风险。

风险表格始终基于原始分析数据计算，即在每个 \code{x_breaks} 时间点，
统计各暴露组中随访时间仍大于等于该时间点的人数。即使绘制调整后曲线，
risk table 也不是模型预测值，而是原始分组的 number at risk。

P 值仅用于未调整曲线：
\itemize{
\item KM 曲线使用 log-rank 检验，即 \code{survival::survdiff()}。
\item CIF 曲线使用 Gray's test，即 \code{cmprsk::cuminc()} 返回的检验结果。
}
调整后曲线不显示 P 值，避免将未调整组间检验误解为调整后模型检验。
}
\examples{
\dontrun{
set.seed(123)
n <- 200

df_km <- data.frame(
  time = runif(n, 1, 60),
  event = rbinom(n, 1, 0.7),
  group = factor(sample(c("A", "B"), n, replace = TRUE)),
  age = rnorm(n, 60, 10),
  sex = factor(sample(c("Female", "Male"), n, replace = TRUE))
)

# 未调整 KM 曲线，显示 risk table 和 log-rank P 值
res_km <- yyds_km(
  data = df_km,
  time_var = "time",
  outcome = "event",
  exposure = "group",
  xlim = c(0, 60),
  x_breaks = seq(0, 60, by = 12),
  risk_table = TRUE
)
res_km$plot

# 调整后 Cox 累计事件概率曲线
res_cox <- yyds_km(
  data = df_km,
  time_var = "time",
  outcome = "event",
  exposure = "group",
  adjusted = c("age", "sex"),
  xlim = c(0, 60),
  x_breaks = seq(0, 60, by = 12),
  risk_table = TRUE
)
res_cox$plot

# 竞争风险 CIF 曲线
df_cif <- data.frame(
  time = runif(n, 1, 60),
  status = sample(0:2, n, replace = TRUE, prob = c(0.25, 0.45, 0.30)),
  group = factor(sample(c("A", "B"), n, replace = TRUE)),
  age = rnorm(n, 60, 10)
)

res_cif <- yyds_km(
  data = df_cif,
  time_var = "time",
  outcome = "status",
  exposure = "group",
  xlim = c(0, 60),
  x_breaks = seq(0, 60, by = 12),
  risk_table = TRUE
)
res_cif$plot

# 固定 y 轴范围
yyds_km(
  data = df_cif,
  time_var = "time",
  outcome = "status",
  exposure = "group",
  xlim = c(0, 60),
  x_breaks = seq(0, 60, by = 12),
  ylim = c(0, 1)
)$plot
}

}

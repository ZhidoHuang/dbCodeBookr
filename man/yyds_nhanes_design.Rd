% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yyds_nhanes_design.R
\name{yyds_nhanes_design}
\alias{yyds_nhanes_design}
\title{创建 NHANES 多周期加权设计对象}
\usage{
yyds_nhanes_design(
  data,
  cycle = NULL,
  weight,
  year_var = "YEAR",
  strata = "SDMVSTRA",
  psu = "SDMVPSU",
  out_weight = "NHANES_wt",
  out_cycle = "NHANES_cycle",
  nest = TRUE,
  lonely_psu = "adjust"
)
}
\arguments{
\item{data}{数据框，包含 NHANES 数据、周期变量、抽样设计变量和候选权重变量。}

\item{cycle}{字符向量，指定需要合并的分析周期，例如 \code{"1999-2018"} 或
\code{c("1999-2002", "2003-2006")}。若为 \code{NULL}，表示不做跨周期合并，
函数会按 \code{YEAR} 分别构造权重，再合并为一个 \code{survey.design} 对象。
若传入多个分析周期，函数会按各分段分别构造权重，再合并为一个
\code{survey.design} 对象。}

\item{weight}{字符向量，候选原始权重变量名，例如
\code{c("WTMEC4YR", "WTMEC2YR", "WTMECPRP", "WTPH2YR")}。}

\item{year_var}{周期标签变量名。默认为 \code{"YEAR"}。期望标签包括
\code{"1999-2000"}、\code{"2017-2020"}、\code{"2021-2023"} 等。}

\item{strata}{分层变量名。默认为 \code{"SDMVSTRA"}。}

\item{psu}{PSU/抽样单元变量名。默认为 \code{"SDMVPSU"}。}

\item{out_weight}{生成的分析权重变量名。默认为 \code{"NHANES_wt"}。}

\item{out_cycle}{生成的分析周期变量名。默认为 \code{"NHANES_cycle"}。}

\item{nest}{传递给 \code{survey::svydesign()} 的逻辑值。默认为 \code{TRUE}。}

\item{lonely_psu}{可选，设置 \code{options(survey.lonely.psu)} 的值。
默认为 \code{"adjust"}；若设为 \code{NULL}，则不修改当前选项。}
}
\value{
返回 \code{survey.design} 对象。生成后的权重变量和分析周期变量会保存在
\code{design$variables} 中；同一份数据保存在 \code{attr(design, "data")}；
权重构造日志保存在 \code{attr(design, "weight_log")}。
}
\description{
根据用户指定的 NHANES 原始权重变量和分析周期，生成多周期分析权重，
并创建 \code{survey::svydesign()} 对象。
}
\details{
本函数不会猜测应该使用哪一类科学权重。用户通过 \code{weight} 显式传入候选原始
权重变量，函数只根据 NHANES 周期规则选择和缩放权重：
\itemize{
\item 1999-2002 使用可用的 \verb{*4YR} 权重。
\item 普通完整两年周期使用可用的 \verb{*2YR} 权重，但不把 \verb{*PH2YR} 当作普通
\verb{*2YR} 权重。
\item 2017-2020 使用可用的 \verb{*PRP} pre-pandemic 权重，并按 3.2 年处理。
\item 2021-2023 若存在 \code{WTPH2YR}，优先使用 \code{WTPH2YR}；否则再尝试普通
\verb{*2YR} 权重。
}
对于合并周期，生成权重的计算公式为：
\code{原始权重 * 该原始权重代表年数 / 分析周期总年数}。
函数会始终在创建 \code{survey::svydesign()} 前剔除生成权重缺失、为 0 或小于
0 的样本，并在控制台简要输出分析周期、权重类型、缩放系数和无效权重数量。
候选权重变量可以是数值型，或可转为数值的字符/因子型变量；空字符串会按缺失处理。
}
\examples{
\dontrun{
des <- yyds_nhanes_design(
  data = dt,
  cycle = c("1999-2018"),
  weight = c("WTMEC4YR", "WTMEC2YR", "WTMECPRP", "WTPH2YR")
)

des_list <- yyds_nhanes_design(
  data = dt,
  cycle = c("1999-2002", "2003-2006"),
  weight = c("WTMEC4YR", "WTMEC2YR")
)
}

}
